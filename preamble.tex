% Some basic packages
\usepackage[T1]{fontenc} % the font specification for some acceented
% characters
% now with XeTeX or LuaTeX, utf-8 is supported automatically
\usepackage{graphicx} %include graphs
\usepackage{ctex} % Chinese Language input support
\usepackage{xcolor} % more colors
%\usepackage{tgpagella} % for TeX Gyre Pagella font
\usepackage{multicol} % 2 columns for space

% Math Environment
\usepackage{amsmath, amsfonts, amssymb, amsthm}
% ams for math, fonts, symbols and theorem
\usepackage{unicode-math} % specify font for math environment

% Environment
% \newtheorem{theorem}{Theorem}
% \newtheorem*{theorem*}{Theorem}
% \newtheorem*{note}{Note}

% input the code
\usepackage{listings}

%\usepackage{syntonly}
%\syntaxonly % use for debugging only

%the pdf output style
\usepackage[bookmarks=true]{hyperref}
\hypersetup{
hidelinks, pdfborder={0 0 0}
}
%title page configuration
\author{Your name}

